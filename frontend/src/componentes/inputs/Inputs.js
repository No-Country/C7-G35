import styled from 'styled-components';

const WrapperInput = styled.div`
    
`;

export const Inputs = () => {
  return (
    <WrapperInput>Inputs</WrapperInput>
  );
};

export const DataList = () => {
  return (
      <div>Inputs</div>
  );
};

export const RadioButton = () => {
  return (
    <div>Inputs</div>
  );
};
